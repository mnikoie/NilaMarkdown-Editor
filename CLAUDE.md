# CLAUDE.md — دستورالعملِ پروژه

## این پروژه چیست

کتابخانهٔ اسنادِ تأمین اجتماعی: بخشنامه‌ها و قوانین از فایلِ Word وارد
می‌شوند، ساختارشان (فصل، ماده، تبصره) استخراج می‌شود، و بعد قابلِ
جست‌وجو، ارجاع و ویرایش می‌مانند.

## وضعیت

> ⚠️ این بخش قبلاً «قدمِ صفر — هیچ تکنولوژی‌ای انتخاب نشده» بود و
> به‌روز نشده بود. عاملِ دیگری این ناسازگاری را پیدا کرد. اعدادِ زیر با
> **مدرکِ واقعی** گرفته شده‌اند (شمارشِ فایل، `ls node_modules`)، نه از
> روی `package.json`.
>
> آخرین به‌روزرسانی: پس از ثبتِ معماریِ منوها در تصمیمِ ۰۰۲۸.

تصمیم‌های پایه **گرفته شده‌اند** و در `docs/decisions/0002` تا `0021`
ثبت‌اند: pnpm، Turborepo، Next.js، NestJS، FastAPI، Postgres،
Meilisearch، MinIO، BullMQ، Auth.js، Playwright.

وضعیتِ واقعیِ هر بخش — «کد دارد» یعنی فایلِ منبعِ خودمان، نه وابستگی:

| بخش | کد | وضعیت |
|---|---|---|
| `packages/markdown` | ۹۰ فایل | ✅ **کامل** — v0.1 تا v0.4، تنها بخشِ پخته |
| `apps/web` | ۷ فایل | 🟡 اسکلت + صفحهٔ نمایشیِ مارک‌داون |
| `apps/api` | ۶ فایل | 🟡 اسکلت |
| `apps/ai-service` | ۱ فایل | 🟡 فقط `main.py` (`.venv` ساخته شده) |
| `apps/worker` | ۰ فایل | ⬜ خالی |
| `packages/{ui,config,shared-types,api-client}` | هرکدام ۱ فایل | ⬜ اسکلت |

**هیچ سرویسِ داکری بالا نیست** (`docker compose ps` فایلی پیدا نکرد).

پس: پایه‌ها انتخاب شده‌اند، ولی جز `packages/markdown` هیچ‌چیز واقعاً
ساخته نشده.

## قواعدِ ثابت

- **تکنولوژیِ تازه را از خودت انتخاب نکن.** اگر کاری به تصمیمی نیاز
  دارد که در `docs/decisions/` نیست، اول بپرس. اضافه‌کردنِ یک
  وابستگیِ تازه یعنی یک تصمیم — حتی اگر کوچک به نظر برسد.
- **هیچ فایل/پوشه‌ای نساز که تصمیمی را تحمیل کند** تا وقتی آن تصمیم
  ثبت شده باشد.
- **بینِ «اعلام‌شده» و «واقعی» فرق بگذار.** بودنِ یک پکیج در
  `package.json` یا نامِ یک سرویس در `docker-compose.yml` **هیچ ثابت
  نمی‌کند**. مدرکِ واقعی: پوشه در `node_modules`، خروجیِ
  `docker compose ps`، وجودِ `.venv`، شمارشِ فایلِ منبع.
  اگر مدرک نداری، بگو «نامعلوم» — ادعای سبزِ اشتباه بدتر از
  بی‌جواب‌بودن است.
- **قبل از ادعای «انجام شد»، خودت تست کن** — از جمله حالتِ خالی و
  حالتِ خطا. گزارشِ موفقیتِ تست‌نشده قابلِ قبول نیست.
- **قبل از پاک/بازنویسیِ چیزی، نگاه کن داخلش چیست.**
- **اگر چیزی را نتوانستی تمام کنی، صریح بنویس چه ماند و چرا.**
  نصفه‌کاره‌ای که «انجام‌شده» گزارش شود، بدترین حالت برای نفرِ بعدی است.

## روشِ کار

- روی دادهٔ واقعی اجرا کن، خروجی را ببین، اصلاح کن. قاعده را از روی
  حدس نساز.
- مرتب commit بزن تا برگشت ممکن باشد. پیامِ commit فارسی است و
  **دلیل** را می‌گوید، نه فقط «چه چیزی».
- هر تصمیمِ معماری را در `docs/decisions/` ثبت کن — با دلیل و
  گزینه‌های ردشده.
- **تستِ سبز یعنی «نشکست»، نه «خوب است».** برای هر چیزی که کاربر
  می‌بیند، به عکسِ واقعیِ صفحه هم نگاه کن. سه دور باگِ دیداری در
  `packages/markdown` پیدا شد که هیچ‌کدام هیچ تستی را قرمز نکرده بودند.

## اگر روی `packages/markdown` کار می‌کنی

`HANDOFF.md` در ریشه را بخوان — قواعدِ خاصِ آن پکیج، چهار تلهٔ محیطی،
و فهرستِ جاهای شکننده را دارد.
