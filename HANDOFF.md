# بریفینگ برای عاملِ بعدی (Codex و مانندش)

> این فایل را کامل بخوان **پیش از** هر تغییری. نوشته شده تا کد به هم
> نریزد و کارِ بعدی بتواند از همین‌جا ادامه پیدا کند.
>
> آخرین به‌روزرسانی: پس از دو‌زبانه‌شدنِ رابط و یکپارچه‌شدنِ سیاستِ تاشدن.

---

## ۱. این پروژه چیست

مونوریپوی `TaminLibrary` — کتابخانهٔ اسنادِ تأمین اجتماعی.

کارِ فعلی روی **یک پکیج** است: `packages/markdown` (`@tamin/markdown`).
یک ویرایشگر و نمایشگرِ Markdown برای React، با تجربهٔ کاربریِ Typora،
مخصوصِ اسنادِ فارسیِ راست‌به‌چپ (فصل، ماده، تبصره).

**وضعیت: v0.1 تا v0.4 تمام است.** همهٔ نقشهٔ
`packages/markdown/PROMPT.md` پیاده شده.

⚠️ **بقیهٔ مونوریپو اسکلت است.** `apps/api` شش فایل دارد،
`apps/ai-service` یکی، و `apps/worker` صفر. تنها بخشِ پختهٔ پروژه همین
پکیج است. وضعیتِ کاملِ هر بخش با مدرک در `CLAUDE.md` هست.

---

## ۲. اول این‌ها را بخوان (به همین ترتیب)

| فایل | چرا |
|---|---|
| `CLAUDE.md` | قواعدِ ثابتِ پروژه — **الزامی** |
| `packages/markdown/README.md` | هر تصمیم و هر عددِ اندازه‌گیری‌شده |
| `packages/markdown/PROMPT.md` | نقشهٔ اصلی و تصمیم‌های بسته |
| `docs/decisions/0022` تا `0029` | دلیلِ تصمیم‌های معماریِ همین پکیج |

`docs/decisions/0024` و `0027` را **حتماً** بخوان اگر به Web Worker یا
تاشدن دست می‌زنی — هر کدام سه تا چهار تلاشِ ناموفق را ثبت کرده‌اند که
همه در مرورگر و **بی هیچ خطایی در کنسول** شکست خوردند.

---

## ۳. قواعدی که نباید بشکنند

این‌ها از `CLAUDE.md` و تصمیم‌های ثبت‌شده می‌آیند:

1. **تکنولوژی را خودت انتخاب نکن.** اگر کاری وابسته به یک تصمیمِ
   نگرفته است، اول بپرس. اضافه‌کردنِ یک وابستگیِ تازه یعنی یک تصمیم.
2. **بینِ «اعلام‌شده» و «واقعی» فرق بگذار.** بودنِ یک پکیج در
   `package.json` هیچ ثابت نمی‌کند. مدرک: پوشه در `node_modules`،
   خروجیِ واقعیِ دستور.
3. **قبل از ادعای «انجام شد» خودت تست کن** — از جمله حالتِ خالی و
   حالتِ خطا.
4. **هر تصمیمِ معماری را در `docs/decisions/` ثبت کن** — با دلیل و
   گزینه‌های ردشده. شمارهٔ بعدی: `0030`.
5. **مرتب commit بزن.** پیامِ commit فارسی است و دلیل را می‌گوید، نه
   فقط «چه چیزی».

### قواعدِ خاصِ خودِ پکیج

6. **حالتِ نمایش هرگز وارد سند نمی‌شود.** تاشدن، پیش‌نمایشِ زنده، حالتِ
   تمرکز — همه `Decoration`اند. اگر واردِ سند شوند،
   `serialize(parse(md)) === md` می‌شکند که قاعدهٔ اصلیِ پکیج است.
7. **`core/` هرگز React را import نمی‌کند** و **`viewer` هرگز
   ProseMirror را**. هر دو با تست اجبار می‌شوند
   (`tests/unit/architecture.test.ts`).
8. **پیش‌فرض همیشه امن است.** HTMLِ خام `escape` می‌شود،
   `javascript:` مسدود است، Mermaid خاموش است (کدِ دلخواه اجرا می‌کند).
9. **هیچ رنگی در TS هارد-کد نمی‌شود** — همه از متغیرِ CSS. تست دارد.
10. **استایل‌ها فقط داخلِ `.tm-editor`.** `preflight.css` عمداً وارد
    نمی‌شود تا صفحهٔ میزبان خراب نشود؛ هر انتخابگرِ بی‌پیشوند این را
    می‌شکند. تست دارد (`typography.spec.ts`).

---

## ۴. ساختار

```
packages/markdown/
  src/
    core/           ← منطقِ خالص، بی React
      markdown/     ← parse و serialize (remark ↔ ProseMirror)
      schema/       ← گره‌ها و مارک‌های ProseMirror
      plugins/      ← live-preview، fold، search، slash-menu، …
      outline/      ← ساختِ درختِ فصل/ماده/تبصره
      highlight/    ← Web Worker رنگ‌آمیزی (تصمیمِ ۰۰۲۴)
      export-html.ts / export-pdf.ts
    node-views/     ← بلوکِ کد، ریاضی، Mermaid، کارتِ مارک
    react/          ← MarkdownEditor، Toolbar، Outline، SearchPanel
    styles/index.css
  tests/unit/       ← ۲۰ فایل، ۳۴۹ تست (Vitest + jsdom)
  scripts/vendor-themes.mjs
tests/e2e/          ← ۷ فایل، ۶۵ تست (Playwright، Chromium واقعی)
apps/web/app/markdown/page.tsx   ← صفحهٔ نمایشی (محصول نیست)
```

---

## ۵. دستورها

```bash
# تستِ واحد
pnpm --filter @tamin/markdown test

# بیلدِ پکیج — بعد از هر تغییر لازم است تا در مرورگر دیده شود
pnpm --filter @tamin/markdown build

# تایپ‌چک
pnpm --filter @tamin/markdown typecheck

# تستِ سرتاسری (سرور باید بالا باشد — بخشِ ۶ را بخوان)
npx playwright test
```

---

## ۶. ⚠️ چهار تلهٔ محیطی که وقت می‌گیرند

این‌ها با تجربه پیدا شده‌اند، نه از روی حدس. هر کدام ساعت‌ها گرفت.

### ۱. سرورِ قدیمی بیلدِ کهنه را سرو می‌کند

**مهم‌ترین تله.** `pnpm build` موفق می‌شود ولی سرورِ در حالِ اجرا هنوز
نسخهٔ قبلی را می‌دهد. چند بار باعث شد دنبالِ باگی بگردیم که وجود نداشت.

چرخهٔ درست برای تستِ بیلدِ تولیدی:

```powershell
# ۱. سرور را بکش
Get-NetTCPConnection -LocalPort 3100 -State Listen -ErrorAction SilentlyContinue |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { Stop-Process -Id $_ -Force }

# ۲. از نو بساز
pnpm --filter '@tamin/markdown' build
Remove-Item -Recurse -Force apps/web/.next
pnpm --filter web build

# ۳. جدا بالا بیاور
Start-Process cmd.exe -ArgumentList "/c","cd /d `"D:\- AIProject\27- TaminLibrary\apps\web`" && npx next start --port 3100" -WindowStyle Hidden
```

بعد: `E2E_BASE_URL=http://localhost:3100 npx playwright test`

> `Start-Process pnpm ... -WindowStyle Hidden` زنده نمی‌ماند. حتماً از
> `cmd /c` استفاده کن.

### ۲. dev و production رفتارِ متفاوت دارند

Turbopack در بیلدِ **تولیدی** فایلِ Web Worker را خام به
`static/media/` کپی می‌کند، بی هیچ پردازشی — پس هر `import()` داخلش
resolve نمی‌شود و هر چانکِ کناری جا می‌ماند. در dev این اتفاق نمی‌افتد.

**هر تغییری در `src/core/highlight/` را روی بیلدِ تولیدی تست کن، نه
فقط dev.** جزئیات در `docs/decisions/0024`.

### ۳. Vitest + jsdom + NodeViewِ نامتقارن قفل می‌شود

تستِ `async` که `EditorView` با NodeViewِ نامتقارن بسازد، ۹۰ ثانیه
هنگ می‌کند و worker را می‌کُشد. برای همین `tests/unit/node-views.test.ts`
عمداً **همگام** است. اگر تستِ تازه‌ای برای NodeView می‌نویسی، همین را
رعایت کن یا رفتارِ نامتقارن را به e2e ببر.

### ۴. ناوبری با کلید در سندِ نمونه غیرقابلِ اتکاست

فرانت‌متر و بلوکِ کد مکان‌نما را جای غیرمنتظره می‌برند. از
`placeCursorInParagraph` در `tests/e2e/markdown-blocks.spec.ts`
استفاده کن، یا روی بلوکِ آماده‌ای که در سند هست کار کن.

---

## ۷. درسِ مهم: تستِ سبز یعنی «نشکست»، نه «خوب است»

سه دور باگِ دیداری پیدا شد که **هیچ‌کدام** هیچ تستی را قرمز نکردند:

- `h1` اندازه‌اش ۱۶px و وزنش ۴۰۰ بود — عینِ پاراگراف.
- فهرست نه نقطه داشت نه شماره؛ نقلِ‌قول بی‌حاشیه بود.
- هر فرمولِ ریاضی **دوبار** چاپ می‌شد.
- فصلِ بی‌فرزند ۱۶ پیکسل جابه‌جا می‌افتاد و مثلِ زیرمجموعه دیده می‌شد.

علتشان یکی بود: تست‌ها **ساختار** را می‌سنجیدند («آیا گرهٔ `h1` ساخته
شد؟») و جواب همیشه بله بود. گره **بود**، فقط بی‌استایل بود.

**اگر تغییری در UI می‌دهی:**
1. `getComputedStyle` را تست کن، نه فقط وجودِ گره — الگو در
   `tests/e2e/typography.spec.ts`.
2. **به عکسِ واقعیِ صفحه نگاه کن**، نه فقط به خروجیِ تست.

---

## ۸. وابستگی‌های اختیاری — بدان که هستند

همه `peerDependency`ِ **اختیاری**‌اند. نبودشان هرگز خطا نمی‌دهد؛ فقط
حالتِ ساده‌تر رندر می‌شود.

| پکیج | برای | اگر نباشد |
|---|---|---|
| `shiki` | رنگ‌آمیزیِ کد | کد خام ولی خوانا |
| `katex` | ریاضی | فرمولِ خام |
| `mermaid` | نمودار | کدِ خام (پیش‌فرض خاموش) |
| `lucide-react` | آیکونِ نوارِ ابزار | — |

⚠️ **CSSِ KaTeX جداست** و مصرف‌کننده باید خودش
`import "katex/dist/katex.min.css"` بزند. بی آن فرمول **خطا نمی‌دهد**
ولی متنِ درهم می‌شود. یک بار همین اتفاق افتاد و مدت‌ها ندیدیم.

---

## ۹. وقتی کارت تمام شد

تا این‌ها را انجام ندهی، کارِ بعدی سخت می‌شود:

- [ ] `pnpm --filter @tamin/markdown typecheck` پاک
- [ ] `pnpm --filter @tamin/markdown test` سبز (۳۴۹ تست)
- [ ] `npx playwright test` سبز روی **بیلدِ تولیدی** (۶۵ تست)
- [ ] اگر تصمیمِ معماری گرفتی → `docs/decisions/0030-…`
- [ ] اگر رفتارِ عمومی عوض شد → `packages/markdown/README.md`
- [ ] commit با پیامِ فارسی که **دلیل** را می‌گوید
- [ ] درختِ git تمیز

**اگر چیزی را نتوانستی تمام کنی، همان‌جا صریح بنویس چه ماند و چرا** —
نه اینکه نصفه‌کاره را «انجام‌شده» گزارش کنی.

---

## ۱۰. جاهایی که شکننده‌اند

اگر دستت به این‌ها خورد، دو برابر دقت کن:

| جا | چرا |
|---|---|
| `core/highlight/` | چهار تله دارد؛ `0024` را بخوان |
| `core/plugins/fold.ts` | سه تلاشِ ناموفق دارد؛ `0027` را بخوان |
| `core/markdown/serialize.ts` | رفت‌وبرگشتِ بی‌تغییر قاعدهٔ اصلیِ پکیج است |
| `styles/index.css` | هر انتخابگرِ بی `.tm-editor` صفحهٔ میزبان را خراب می‌کند |
| `tsup.config.ts` | بیلدِ دومِ worker شرایطِ دقیقی دارد؛ کامنت‌هایش را بخوان |

---

## ۱۱. چیزهایی که عمداً این‌طورند — عوضشان نکن

اگر یکی از این‌ها را «اشکال» دیدی، اول بپرس:

- **`mermaid` پیش‌فرض خاموش است** — دلیلش امنیت است نه کارایی، پس با
  Web Worker هم روشن نمی‌شود.
- **فهرستِ زبان‌های Shiki دستی است** (۱۳ تا در
  `core/highlight/langs.ts`). فهرستِ کاملِ Shiki فایلِ worker را ۹٫۷
  مگابایت می‌کرد. زبانِ بیرونِ فهرست خطا نیست — خام می‌ماند.
- **جدول تنها جایی است که خروجی عیناً ورودی نیست** (remark ردیفِ
  جداکننده را نرمال می‌کند). شکلِ نرمال‌شده پایدار است.
- **دونقطه در `data:` escape می‌شود** (`data\:image`). درست است —
  CommonMark می‌فهمدش و رفت‌وبرگشت سالم می‌ماند.
- **`preflight.css` وارد نمی‌شود.** جایش با استایلِ `.tm-editor` پر شده.
